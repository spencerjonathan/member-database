<?xml version="1.0" encoding="utf-8"?>
<form>
	<fieldset>
		<field
				name="id"
				type="hidden"
				/>
		<field
				name="tower_id"
				type="sql"
				label="Tower"
				query="select id as value, concat(place, ' (', designation, ')' ) as tower_id from #__md_tower"
				/>		
		<field
				name="user_id"
				type="sql"
				label="User"
				query="select id as value, concat(username, ' (', name, ')' ) as user_id from #__users"
				/>
	</fieldset>
</form>
